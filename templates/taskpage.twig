{% extends 'base.twig' %}
{% block content %}
	<ul>
		{% for task in tasks %}
			<li>
				<a href="/todo_list/public/task/{{ task.id }}">{{ task.title }}</a>
			</li>
		{% endfor %}
	</ul>
{% endblock %}
